import { decode } from 'base64-arraybuffer';
import { thumbHashToDataURL } from 'thumbhash';

function decode2url(hash) {
  const arrayBuffer = decode(hash);
  return thumbHashToDataURL(new Uint8Array(arrayBuffer));
}

export { decode2url };
